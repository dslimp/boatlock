# BoatLock Firmware 0.2.5 Release Notes

## Fixed

- Automated Android latest-release OTA now waits until BLE command and OTA
  characteristics are both bound before starting the firmware upload.
- Android/macOS app package version is now `0.2.5+5`.

## Validation

- Firmware version string changed to `0.2.5`.
- Release artifacts are still published only from `v*` tags through GitHub
  Releases.
