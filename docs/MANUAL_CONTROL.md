# Manual Control Mode

Manual control is no longer exposed in the main Flutter app flow.

The firmware command surface still contains manual commands for service and
feature-branch work, but the release-oriented `main` app is limited to anchor
operations and emergency stop.
